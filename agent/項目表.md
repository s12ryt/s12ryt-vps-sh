# 項目表

## 現有檔案

| 路徑 | 用途 | 主要依賴 |
| --- | --- | --- |
| `需求.md` | 使用者原始需求 | 無 |
| `he-ipv6.md` | v1.1.0 多 IPv6 出站與 Web 面板原始概念需求 | 無 |
| `agent/question.md` | 已確認的需求、約束與驗收契約 | `需求.md`、需求澄清紀錄 |
| `agent/deep_todos.md` | 任務歷史與進度 | `agent/question.md` |
| `agent/項目表.md` | 檔案與目錄職責、相依關係 | 全專案 |
| `agent/memory.md` | 本次及後續操作紀錄 | 全專案 |
| `.gitattributes` | 強制 Bash、Bats 與 Actions 檔案使用 LF | Git |
| `.gitignore` | 排除本機暫存資料 | Git |
| `.github/workflows/ci.yml` | GitHub-hosted Go format/test/vet、Bats、ShellCheck、Bash 語法、文件及 10 發行版容器煙霧驗證 | GitHub Actions、Go、Bats、Docker |
| `README.md` | 安裝、功能、支援範圍、限制、手動煙霧測試及學習參考 | 專案行為 |
| `LICENSE` | canonical GPL-3.0-only 全文 | SPDX license-list-data |
| `tests/test_helper.bash` | Bats 隔離測試目錄與主選單執行 helper | Bats |
| `tests/main.bats` | 主選單、錯誤輸入及非 root `s` 安裝契約 | `s12ryt.sh` |
| `tests/menu_flow.bats` | 互動 TTY 清除、單鍵返回、非互動略過與頂層功能流程契約 | `s12ryt.sh`、Bats mock |
| `tests/bootstrap.bats` | process substitution、自我安裝重新下載與失敗降級契約 | `s12ryt.sh`、curl mock |
| `tests/system.bats` | 系統資訊及跨套件管理器一般升級契約 | `s12ryt.sh`、mock 命令 |
| `tests/network.bats` | IP 分類、連通性及有限服務地區解析契約 | `s12ryt.sh`、curl mock、JSON fixture |
| `tests/proot.bats` | PRoot 路徑、映像、架構、管理操作與 helper 保護契約 | `install-proot.sh`、`s12ryt.sh`、mock 命令 |
| `tests/proot_setup.bats` | PRoot 主機依賴、Python 選擇與隔離安裝契約 | `install-proot.sh`、mock 命令 |
| `tests/supervisor.bats` | 客體選擇、Supervisor 安裝、服務操作及發行版設定路徑契約 | `install-proot.sh`、`s12ryt.sh`、mock 命令 |
| `tests/fanout.bats` | Fanout 前置條件、下載、語法驗證及直接執行契約 | `s12ryt.sh`、mock 命令 |
| `tests/update.bats` | 語意版本、Release 查詢、自我更新失敗保護及項目列表契約 | `s12ryt.sh`、curl mock |
| `tests/node_install.bats` | NodeSource Node.js 版本、確認、權限、平台/架構與失敗保護契約 | `s12ryt.sh`、mock 命令 |
| `tests/python_install.bats` | 私有 uv、Python 版本、受管 pip、固定 venv、既有版本補齊與失敗保護契約 | `s12ryt.sh`、mock 命令 |
| `tests/container-smoke.sh` | 發行版容器內的語法、套件管理器與 x86_64 映射煙霧檢查 | `s12ryt.sh`、`install-proot.sh` |
| `tests/fixtures/` | 系統與網路解析使用的決定性測試資料 | Bats |
| `s12ryt.sh` | 主選單、TTY 清除與返回流程、實體/暫時來源自我安裝及其餘功能入口 | Bash、curl、`install-proot.sh` |
| `install-proot.sh` | PRoot 工具鏈、客體生命週期與 Supervisor 服務管理 | proot、proot-distro 5.5.0、OCI rootfs、Supervisor |
| `install-ipv6.sh` | 多 IPv6 專案 Release 資產校驗、首次安裝、更新回滾、狀態與卸載 | GitHub Release、sing-box v1.13.15、systemd/OpenRC |
| `tests/ipv6_project.bats` | 主選單 8、項目子選單與 helper 接線契約 | `s12ryt.sh`、`install-ipv6.sh`、mock |
| `tests/ipv6_install.bats` | 多 IPv6 資產、服務安裝、更新健康回滾、設定與卸載契約 | `install-ipv6.sh`、mock 系統命令 |

## v1.1.0 Go 與 Web 結構

| 路徑 | 用途 | 主要依賴 |
| --- | --- | --- |
| `go.mod` | Go 面板模組與可重現相依版本 | Go toolchain |
| `cmd/s12ryt-ipv6/` | 面板單一 binary 入口 | `internal/`、嵌入式 Web 資產 |
| `internal/domain/` | 節點、出口模式、拓撲、輪換與設定模型 | Go 標準庫 |
| `internal/store/` | schema 化 root-only JSON、原子寫入與備份 | `internal/domain/` |
| `internal/auth/` | 密碼雜湊、session、CSRF、登入限速與敏感值解鎖 | Go crypto、`internal/store/` |
| `internal/panel/` | HTTP API、繁中模板、嵌入式 CSS/JS 與安全 middleware | `internal/domain/`、`internal/auth/` |
| `internal/singbox/` | sing-box 七協議設定、TLS transport 與 Reality 設定生成 | `internal/domain/` |
| `internal/importer/` | 遠端 outbound URI、JSON 與 Base64 匯入驗證 | Go 標準庫 |
| `internal/share/` | 本機節點 URI、client JSON、QR payload 與 Base64 訂閱 | Go 標準庫 |
| `internal/routing/` | 新連線出口輪換、健康狀態與週期檢查排程 | Go 標準庫 |
| `internal/network/` | IPv6 探測/生成、地址命令與防火牆計畫 | `internal/domain/`、系統命令介面 |
| `internal/service/` | systemd/OpenRC、更新回滾、日誌與生命週期 | `internal/network/`、`internal/store/` |
| `internal/nodes/` | 節點認證、交易式 CRUD 與受保護設定替換 | `internal/domain/`、`internal/network/` |
| `internal/certificates/` | 自簽、外部憑證驗證與 TLS 部署計畫 | Go crypto/x509 |
| `internal/system/` | 白名單命令交易與反序回滾 | Go 標準庫 |
| `internal/deploy/` | 候選設定的多元件 prepare/apply/health/rollback 協調 | `internal/domain/` |
| `internal/runtimeconfig/` | domain 到 sing-box 編譯、`sing-box check` 與 0600 原子配置交易 | `internal/singbox/`、`internal/deploy/` |
| `tests/e2e/` | Playwright 桌面/手機、登入、導覽、modal 與響應式驗收 | GitHub-hosted browser、panel binary |
